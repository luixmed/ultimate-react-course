import styled from "styled-components";

export const CartOverviewStyled = styled.div`
  /* Font */
  color: #e7e5e4;
  font-weight: 600;
  text-transform: uppercase;

  /* Box */
  padding: 1.6rem 2.4rem;

  /* Layout */
  display: flex;
  justify-content: space-between;
  align-items: center;

  /* Style */
  background-color: #292524;

  p {
    color: #d6d3d1;

    span {
      margin-right: 2.4rem;
    }
  }

  a {
    color: inherit;
  }
`;
